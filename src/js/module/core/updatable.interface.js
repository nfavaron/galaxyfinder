/**
 * Update interface
 *
 * @constructor
 */
var UpdatableInterface = function () {

};

/**
 * Update the instance state at a given @time since the start
 *
 * @param {number} time Number of milliseconds elapsed since the start
 * no @returns
 */
UpdatableInterface.prototype.update = function (time) {

    // Extend to implement your logic
};

module.exports = UpdatableInterface;